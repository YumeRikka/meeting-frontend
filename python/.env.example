# 腾讯会议自建应用凭证（腾讯会议官网 → 用户中心 → 企业管理 → 高级 → REST API 复制）
# 只存在于本机，不提交 / 不截图 / 不发群里
APPID=your_appid_here
SDKID=your_sdkid_here
SECRET_ID=your_secret_id_here
SECRET_KEY=your_secret_key_here

# 要监听的微信群名（必须和微信里显示的一字不差）
LISTEN_GROUP=你的群名

# 腾讯会议主持人 userid（企业通讯录里的真实 userid，不是手机号）。单账号场景可用这个；多账号请改用下方 MEETING_ACCOUNTS
MEETING_USERID=your_host_userid_here

# 多会议账号（同一企业内）。格式：显示名:userid，多个用英文逗号分隔
# 机器人建会时会遍历这些账号，挑选「请求时段前后各 CONFLICT_BUFFER 秒」内无冲突的账号来建会
# 例：MEETING_ACCOUNTS=主账号:uid1,副账号:uid2
MEETING_ACCOUNTS=

# 冲突缓冲（秒）：新会议「前后各多少秒」内不得与已有会议重叠。默认 1800（即半小时）
CONFLICT_BUFFER=1800

# 主持人密钥（可选，通常 6 位数字）。留空则不设。
# 注意：host_key 部分腾讯会议套餐需要企业版/商业版才支持，免费版可能报错。
HOST_KEY=

# ===== 网页登录建会（绕过「创建会议」12 次/月限频）=====
# 真正建会改为「驱动腾讯会议网页端登录账号」完成，可设会议名称 + 主持人密钥，且不占 REST 创建配额。
# 各账号的登录账号(手机/邮箱)与密码写在同目录的 accounts.json（git 忽略，切勿提交/外发）。
# 首次需为每个账号执行一次：python create_via_web.py --login <userid>
# method 可选 web（默认，优先网页建会）/ rest（仅用 REST API，受 12 次/月限制）。
CREATE_METHOD=web

# 触发词：艾特机器人后，消息以此开头才建会
TRIGGER=建会

# 取消会议触发词：艾特机器人后，消息含此词即视为取消指令（需附会议号）。可改，如 "取消"
CANCEL_TRIGGER=取消会议

# 可选：机器人在群里的显示昵称。
# 填了之后只响应「艾特到这个名字」的消息（更精准，防止别人互艾特误触发）；
# 留空则任何艾特 + 触发词 都会响应。
BOT_NICKNAME=

# ===== H5 卡密自助建会（python/h5/）专用 =====
# 管理员令牌：生成卡密接口需要它。务必改成你自己的强随机串！
ADMIN_TOKEN=请改成你自己的管理员令牌
# 卡密数据库路径（SQLite）。默认 h5/cards.db，一般不用改。
# CARDS_DB=D:\WorkBuddy\meeting-bot\h5\cards.db
